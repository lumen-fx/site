[project]
name = "lumenfx-site"
version = "0.1.0"
description = "Marketing site and unified docs for Lumen and Candela (lumenfx.dev)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.47",
]
